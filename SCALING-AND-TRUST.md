# 世界 Online 扩容与可信开发者方案

## 当前版本已经落实

- 游戏外壳是可安装 PWA。HTML、脚本、样式和小型依赖使用版本化缓存；核心文件联网优先，断网时才读取缓存。
- 本地大模型在独立 Worker 中运行。流式输出降频，离开导引或页面隐藏时终止本轮生成，默认推荐 1.5B 而非 3B。
- 实时聊天收到新消息时只追加一个消息节点，不重建输入框。客户端最多保留 80 条消息。
- 登录提醒显示客户端版本和实际开放的服务器。当前只有 `gold-1`，因此服务器列表不会展示不可用的假服务器。

这些措施能显著减少首屏流量和单机卡顿，但静态托管加单个 Supabase 项目不能对无限并发作出保证。

## 热门模式扩容路径

1. 静态资源放在带边缘缓存的对象存储/CDN，文件名带内容哈希。HTML 短缓存，模型权重、WASM、音乐和图片长缓存且不可变。
2. 服务器注册中心只返回健康且未满载的分区。每个角色固定 `server_id`；换服先显示角色是否存在，禁止把本地存档误当成跨服角色。
3. 聊天表增加 `server_id`，索引采用 `(server_id, channel, created_at desc)`。客户端只订阅当前服务器和当前频道，不再接收全库广播。
4. 战斗、奖励、玩家代表大会和人物之王代表由服务器权威结算。客户端只提交带递增序号的意图；重复序号、过期请求和越权动作直接拒绝。
5. 网关执行每 IP、账号、设备和功能维度限流。非关键的在线人数、历史列表和 AI 摘要允许降级；奖励结算与存档写入不降级。
6. 热点房间按 `server_id + room_id` 一致性哈希到有状态进程。进程无本地永久数据，事件日志写入队列，消费者异步生成排行榜和监控指标。
7. 自动扩容依据连接数、事件队列延迟、数据库连接池等待和 P95 响应时间，而不是只看 CPU。扩容前必须做 1 倍、3 倍和 10 倍目标峰值压测。

建议的容量保护线：聊天 P95 小于 300ms，战斗意图 P95 小于 150ms，错误率小于 0.5%，队列等待小于 2 秒。超过保护线时先停止非关键 AI 摘要和历史回放，再排队进入热门房间，不能牺牲存档一致性。

## 开发者身份

`sss` 和 `pxz` 只能作为允许申请开发者身份的显示名，绝不能直接决定权限。否则任何玩家改名即可接管开发功能。

可信流程如下：

1. 这台电脑用 WebAuthn/Passkey 生成不可导出的设备凭证。
2. 服务器保存公钥、凭证 ID、授权人、授权时间、撤销时间和最小权限集合，不保存私钥。
3. 登录时服务器发出一次性 challenge，验证签名后在短期会话中签发 `role=developer`。名称不是权限条件。
4. 新设备必须由已授权设备或控制台人工批准；丢失设备可立即撤销。普通名称登录仍签发 `role=player`。
5. 所有开发操作写入不可修改的审计日志，并要求二次确认。客户端界面隐藏按钮不算权限控制。

在挑战签名验证的服务端函数部署之前，当前版本不会开放开发者模式。这是有意的安全边界。

## AI 巡检边界

- 客户端始终进行廉价确定性检查：未处理异常、长任务、掉帧、重复奖励请求、非法状态转换和越界输入。
- 只上传去标识化、采样后的错误事件；聊天正文、Passkey、存档全量和模型对话不进入遥测。
- 小金牛仔在开发者明确打开报告时分析聚合指标。它不会在后台持续运行本地大模型，否则会再次占用 GPU 并影响玩家输入。
- 阿比盖尔、人物之王代表和小金牛仔可以共用同一套本地模型运行时，但同一设备最多一个生成任务。游戏结果仍由白名单规则或服务器权威逻辑决定，模型只能生成分析和发言。

## 玩家代表大会

代表大会需要服务端房间、提案状态机、投票截止时间、幂等投票和审计记录。人物之王以 `representative_ai` 服务账号发言，但不能投票、修改提案或直接执行结果。模型输出先经过长度、权限和内容边界检查，再作为一条普通发言广播。客户端静态页面不能独自实现可信大会。

## 应用商城发布

当前 PWA 可以被玩家真实安装和离线启动，但不等于已上架 Microsoft Store、Google Play 或 App Store。商城发布还需要用户持有的开发者账号、签名证书、隐私政策、年龄分级、商店素材和审核提交。推荐先用 PWA 验证稳定性，再用受信任 Web Activity 或桌面壳打包；签名密钥必须由账号所有者保管，不能写入仓库或交给网页客户端。
